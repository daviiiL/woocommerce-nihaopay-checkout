# Load NVM if not already loaded (update the path if your nvm directory is different)
export NVM_DIR="$HOME/.nvm"
[ -s "$NVM_DIR/nvm.sh" ] && source "$NVM_DIR/nvm.sh"

# Use Node version from .nvmrc if it exists
if [ -f .nvmrc ]; then
  nvm use
fi

